// legacy configuration file retained for compatibility.
// All URL configuration is now managed in `src/utils/env.js`.

import { API_BASE_URL } from './utils/env';
export { API_BASE_URL };

// NOTE: the above export keeps existing imports working but you should
// migrate to using the `env` module directly.
